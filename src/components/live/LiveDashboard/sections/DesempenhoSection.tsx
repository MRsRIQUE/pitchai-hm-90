import { AiAutomationsDashboard } from "../../AiAutomationsDashboard";

/** Leitura pura: os números que a extensão raspa do Gerenciador de LIVE. */
export function DesempenhoSection() {
  return (
    <div className="app-section">
      <AiAutomationsDashboard />
    </div>
  );
}
