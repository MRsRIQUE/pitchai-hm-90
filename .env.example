# Servidor (Firebase):
# - FIREBASE_SERVER_EMAIL e FIREBASE_SERVER_PASSWORD devem ser a conta do
#   servidor: crie-a no Firebase Console (Authentication > Users) com o email
#   "pitchaihm-server@gen-lang-client-0224756084.firebaseapp.com" (senha forte).
#   O backend usa essa conta para acessar o Firestore com as regras isServer().
FIREBASE_SERVER_EMAIL=
FIREBASE_SERVER_PASSWORD=

# AI (Gemini/TTS)
GEMINI_API_KEY=
LOVABLE_API_KEY=

# Pagamentos
VITE_PAYMENTS_CLIENT_TOKEN=
PERFECTPAY_TOKEN=
PERFECTPAY_INTEGRATION_TOKEN=
# PERFECTPAY_TOKEN e obrigatorio para o webhook /api/payments ser funcional.
# Sem ele, o webhook retorna 503 (server_misconfigured) e rejeita todos os POSTs.
# IMPORTANTE: o valor "27b7e14980381ae6d539d478f9992ce9" foi hardcoded no
# historico do git (commit 7638ec2) e DEVE ser rotacionado — nunca reusar.

# E2E (Playwright)
E2E_BASE_URL=
E2E_ALUNO_EMAIL=
E2E_ALUNO_PASSWORD=